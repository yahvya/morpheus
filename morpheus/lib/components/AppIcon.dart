import 'dart:io';
import 'package:flutter/cupertino.dart';

/// icône de l'application
class AppIcon extends StatelessWidget{
  const AppIcon({super.key});

  @override
  Widget build(BuildContext context){
    return Image(image: Image.file(File("") ).image );
  }
}
